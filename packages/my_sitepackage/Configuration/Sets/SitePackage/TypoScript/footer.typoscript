# Load and render footer content from the sysfolder
# Uses the classic CONTENT object (proven and stable)
lib.footer = CONTENT
lib.footer {
    table = tt_content
    select {
        pidInList = {$footer.storagePid}
        where = CType='gripsraum_footer'
        max = 1
    }
}
