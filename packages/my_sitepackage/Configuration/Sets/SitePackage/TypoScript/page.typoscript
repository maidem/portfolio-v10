##############
#### PAGE ####
##############
page = PAGE
page {
    typeNum = 0
    shortcutIcon = EXT:my_sitepackage/Resources/Public/Icons/favicon.ico

    10 = PAGEVIEW
    10 {
        paths {
            0 = EXT:my_sitepackage/Resources/Private/PageView/
            1 = {$page.pageview.paths}
        }
        variables {
            # nav_title overrides the wordmark if set (e.g. root page = "Über mich"),
            # otherwise falls back to the regular page title.
            pageTitle = TEXT
            pageTitle.data = page:nav_title // page:title

            # Expose current page uid for active-link detection in Fluid layouts
            pageUid = TEXT
            pageUid.data = page:uid

        }
        dataProcessing {
            10 = files
            10 {
                references.fieldName = media
            }
            20 = menu
            20 {
                levels = 2
                includeSpacer = 1
                as = mainnavigation
            }
            30 = page-content
        }
    }

    meta {
        viewport = {$page.meta.viewport}
        robots = {$page.meta.robots}
        apple-mobile-web-app-capable = {$page.meta.apple-mobile-web-app-capable}

        theme-color = #ffffff
        theme-color {
            attribute = name
        }

        X-UA-Compatible = {$page.meta.compatible}
        X-UA-Compatible {
            attribute = http-equiv
        }
    }

    # Cross-Document View Transitions. Muss im <head> stehen und beim Parsen des
    # neuen Dokuments schon da sein — im Fluid-Layout landet es im <body> und
    # wird ignoriert, im per Vite geladenen CSS kommt es zu spaet.
    headerData {
        10 = TEXT
        10 {
            value (
                <style>@view-transition { navigation: auto; }</style>
            )
        }
    }

    includeCSSLibs {

    }

    includeCSS {

    }

    includeJS {

    }

    includeJSFooter {
        # my_sitepackage_scripts = EXT:my_sitepackage/Resources/Public/JavaScript/main.js
    }

    footerData {
        10 = TEXT
        10 {
            value (
                <script>
                  var _paq = window._paq = window._paq || [];
                  _paq.push(['disableCookies']);
                  _paq.push(['trackPageView']);
                  _paq.push(['enableLinkTracking']);
                  (function() {
                    var u="https://matomo.maidem.de/";
                    _paq.push(['setTrackerUrl', u+'matomo.php']);
                    _paq.push(['setSiteId', '1']);
                    var d=document, g=d.createElement('script'), s=d.getElementsByTagName('script')[0];
                    g.async=true; g.src=u+'matomo.js'; s.parentNode.insertBefore(g,s);
                  })();
                </script>
            )
        }
    }

}

