form.contact.recipientEmail: connect@maidem.de
form.contact.privacyPageUid: 15
# Mosparo credentials are injected into the site-set TypoScript chain via
# packages/my_sitepackage/ext_localconf.php → addTypoScriptSetup() from
# environment variables (MOSPARO_PUBLIC_SERVER, MOSPARO_VERIFY_SERVER,
# MOSPARO_UUID, MOSPARO_PUBLIC_KEY, MOSPARO_PRIVATE_KEY).
# Set these in Coolify (Available at Runtime ✓) or in
# .ddev/config.local.yaml `web_environment` for local development.
